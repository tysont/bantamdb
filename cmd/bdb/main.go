// ABOUTME: CLI entrypoint for BantamDB. Wires together the database layers
// ABOUTME: and starts the server.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "bdb: not yet implemented")
	os.Exit(1)
}
